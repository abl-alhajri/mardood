# =============================================
# XYZTRADINGAE — Python Dependencies
# =============================================
# All HTTP integrations (Coinbase, CoinGecko, mempool.space, alternative.me,
# Telegram, Hugging Face, Reddit) go through `requests`. The previous
# wrapper packages (yfinance, alpaca-py, python-binance, pycoingecko,
# python-telegram-bot, pandas-ta, newsapi-python, plotly, kaleido) were
# never imported and are gone.

# Core
anthropic>=0.40.0          # Claude API (needs cache_control + tool_use support)
python-dotenv>=1.0.0       # Load .env file
flask>=3.0.0               # Web dashboard
gunicorn>=21.2.0           # Production WSGI server (used by run.py supervisor)

# Data + analysis
pandas>=2.0.0
numpy>=1.26.0
requests>=2.31.0

# Scheduling & terminal UI
schedule>=1.2.1
pytz>=2024.1
rich>=13.7.0

# Telegram command bot (outbound alerts use raw HTTP via requests in
# alerts/notifier.py; this dep is purely for the polling Application
# that handles /status, /positions, /signals, /portfolio).
python-telegram-bot==21.7

# SQLite is built into Python — no extra install needed.
